cat > .github/workflows/auto-push-pr.yml << 'EOF'
name: 🚀 Auto Push & PR
on:
  workflow_dispatch:  # 버튼 클릭으로 실행

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        token: ${{ secrets.GITHUB_TOKEN }}
    
    - name: 🔧 Setup GitHub CLI
      uses: github/gh-cli-setup@v1
      
    - name: ⚙️ Auto-config Git CLI
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        gh auth login --with-token < <(echo "$GH_TOKEN")
        gh auth status
    
    - name: 📝 Auto commit & push
      run: |
        chmod +x .github/scripts/auto-deploy.sh
        .github/scripts/auto-deploy.sh
    
    - name: 🔗 Auto create PR
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD)
        gh pr create \
          --base main \
          --head "$CURRENT_BRANCH" \
          --title "🚀 Auto Deploy PR from $CURRENT_BRANCH" \
          --body "자동 배포 스크립트 실행 결과입니다.\n\n$(date)" \
          --web
EOF